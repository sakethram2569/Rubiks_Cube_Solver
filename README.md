# Rubik's Cube Solver

A 3x3 Rubik's Cube solver implementing BFS, DFS, IDDFS, and Korf's IDA*
algorithm with corner pattern databases, built from scratch in C++.

## Status
🚧 In progress — Phase 0: project setup

## Build
\`\`\`
mkdir build && cd build
cmake ..
make
./cube_solver
\`\`\`